# BattleAction.gd

# Эта строка регистрирует класс BattleAction в Godot
# Теперь другие скрипты могут его использовать
class_name BattleAction

# Наследуемся от Resource - это позволяет сохранять/загружать наши действия
extends Resource

# Объявляем переменные (свойства) которые будут у каждого действия
var action_name: String        # Название действия (например, "Атака")
var sync_required: int = 0     # Нужный уровень доверия для действия
var sync_change: int = 0       # Как изменится доверие после выполнения
var refusal_penalty: int = -10 # Штраф к доверию при отказе

# Эта функция будет выполняться когда действие используют
func execute(caster, target) -> bool:
	# Пока просто выводим сообщение в консоль
	print("Выполняется действие: " + action_name)
	# Возвращаем true - действие выполнено успешно
	return true
